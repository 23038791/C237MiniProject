<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Jack's Personalised Affirmation App</title>
</head>

<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Personalised Affirmation App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/addAffirmer">Add affirmer</a>
          </li>
          <li>
            <a class="nav-link" href="/talk">Feedback</a>
          </li>
          <li>
            <a class="nav-link" href="/register">Register</a>
          </li>
          <li>
            <a class="nav-link" href="/login">Login</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center">
      <h2>Update Affirmer</h2>
    </div>
    <br>
    <div class="text-center">
      <% if (affirmer) { %>
        <form action="/submit/<%= affirmer.affirmer_id %>" method="POST" enctype="multipart/form-data">
          <label for="name">Affirmer name:</label><br>
          <input type="text" id="name" name="name" value=<%=affirmer.name %> required><br><br>

          <label for="contact">Contact:</label><br>
          <input type="number" id="contact" name="contact" min="80000000" max="99999999" value=<%=affirmer.contact %>
          required><br><br>

          <label for="specialty">Specialty:</label><br>
          <input type="text" id="specialty" name="specialty" value=<%=affirmer.specialty %> required><br><br>

          <label for="image">Current Image:</label><br>
          <input type="text" name="currentimage" value=<%= affirmer.image %> disabled><br>
          <img src="/image/<%= affirmer.image %>" width="20%"><br><br>

          New Image:<br>
          <input type="file" id="image" name="image" accept="image/*" value=<%=affirmer.image%><br><br>
          <br>
          <button type="submit">Update Affirmer</button>
        </form>
        <% } else { %>
          <p>No Affirmer found.</p>
          <% } %>
            <br>
            <a href="/">Back</a>
    </div>
  </div>

</body>

</html>